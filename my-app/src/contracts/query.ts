export type PostData = {
    id: number,
    title: string,
    body: string,
};

export type CommentData = {
    id: number,
    name: string,
    body: string,
    email: string
};

export type BirdData = {
    image: string,
    fact: string
};